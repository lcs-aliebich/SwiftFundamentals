//: # operators and conditions
//: [view tutorials](https://www.hackingwithswift.com/100/swiftui/3)
/*: - Note:
Remember to take the practice tests for each topic.
 \
\
Please [commit your work frequently](https://www.russellgordon.ca/cs/source-control/introduction/using-source-control.pdf) and document your progress once each day in [Spaces](http://ca.spacesedu.com).
 */

import Cocoa

var str = "Hello, playground"

let firstscore = 7
let secondscore = 8
let total = firstscore + secondscore
let difference = secondscore - firstscore
let product = firstscore * secondscore
let divided = firstscore / secondscore
let remainder = 13 % secondscore

let meaningOfLife = 42
let doublemeaning = 42 + 42
let fakers = "fakers gonna"
let action + "fake"
let firsthalf = ["john", "Paul"]
let secondhal = ["george", "Ringo"]
let beatles = firsthalf + secondhalf

var score = 95
score -= 5
var quote = "The rain in sapin falls mainly on the"
quote += "spainiards"

let firstscore = 6
let secondscore = 4
firtscore == secondscore //this checks if two scores are equal if (!) pops up they are nopt equal
firstscore != secondscore
firstscore < secondscore
firstscore >= secondscore
"Taylor" <= "Swift"

let firstcard = 11
let secondcard = 1-

if first card + second card == 21 {
    print("blackjack!")
} else {
    print("Regular cards")
    
}
let age1 = 12
let age2 = 21

if age1 > 18 && age2 > 18 {
    print("Both are over 18")
}

if age1 > 18 || age2 > 18 {
    print("At least one is over 18")
}

let firstcard = 11
let secondcard = 10
print (firstcard == secondcard ? "cards are the same" : "cards are different")

if firsrcard == secondcard {
    print("cards are the same")
} else {
    print("cards are different")
}

let weather {
    case "rain":
print("Bring an umbrella")
    case "snow":
print("wrap up warm")
case "sunny":
    print("wear sunscreen")
default:
    print("enjoy your day!")
}

let score = 85
switch score {
case 0..<50:
    print("You failed badly")
case 50..<85:
    print("You did ok.")
default:
    print("You did great!")
}


